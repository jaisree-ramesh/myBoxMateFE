# myBoxMateFE

# React + TypeScript + Vite

# Start local development: 

npm run dev

# Start Json-server (just to test the spaces - it´s gonna be removed later):

npx json-server --watch db.json --port 3000